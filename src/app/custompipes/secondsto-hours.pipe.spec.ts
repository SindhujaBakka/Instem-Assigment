import { SecondstoHoursPipe } from './secondsto-hours.pipe';

describe('SecondstoHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondstoHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
